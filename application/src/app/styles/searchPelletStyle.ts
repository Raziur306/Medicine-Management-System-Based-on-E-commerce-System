import { Box, styled } from "@mui/material";

export const StyledParentBox = styled(Box)({
    position: 'absolute',
    top: 105,
    left: 380,
    background: 'white',
    minHeight: 590,
    minWidth: 500,
    color: "black"
})